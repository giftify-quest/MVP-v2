import { SectionTitle } from "@/components/reusableComponent/section-title/SectionTitle";

export const SectionFreeInput = () => {
  return (
    <div>
      <SectionTitle variant="purple" mainWord="need to get in props" />
    </div>
  );
};
