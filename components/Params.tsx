'use client'

import React from "react";
import { Params as ParamsPropsType} from "@/types/Params"

interface ParamsProps {
    params:ParamsPropsType
}

const ParamsComponent : React.FC<ParamsProps> = ({params}) => {

    return(
        <div>

        </div>
    )
}

export default ParamsComponent